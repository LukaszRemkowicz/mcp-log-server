"""Application middleware package for shared MCP request handling.

This package holds cross-cutting wrappers that should live above the
deterministic tool layer, such as authenticated request audit logging.
"""
