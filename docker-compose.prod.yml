services:
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PYTHONPATH: /app/src
    ports:
      - "8001:8001"
    command: ["uv", "run", "python", "-m", "main"]
