feat(mcp): Add snapshot-based log analysis tools

Replace inline-oriented log collection with a snapshot-first workflow that
persists workflow and session log snapshots, then lets agents inspect them
through list/read/grep follow-up tools.

Refactor collection logic into dedicated services, models, utils, and error
mapping helpers so the MCP tool layer is thinner and easier to reason about.
Tighten the agent-facing contract with clearer auth-driven discovery defaults,
chunked snapshot reads, paged grep windows, improved snapshot validation
errors, and stronger live HTTP coverage in CI.
