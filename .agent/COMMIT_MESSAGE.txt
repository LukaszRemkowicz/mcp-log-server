feat(mcp): Add Dockerized MCP service foundation and repo automation

Build the initial MCP service foundation with a flat src layout, uv-based
workflow, Docker Compose runtime, hot reload, and basic HTTP endpoints for
status, health, and POST testing. Add a temporary auth abstraction so the
service can grow toward Keycloak without coupling the early code to the final
provider.

Document the architecture and repository foundation, and add repository
automation for pre-commit, CI, CodeQL, and VERSION-based tag workflows so the
project follows the same release discipline as landingpage from the start.
